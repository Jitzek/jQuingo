import { ObservableValue } from "@src/jquingo/observable_value";

export const amount_of_rows_observable: ObservableValue<number> = new ObservableValue<number>(5);
export const amount_of_columns_observable: ObservableValue<number> = new ObservableValue<number>(5);